<!DOCTYPE html>
<html>
  <head>
    <title><%=  "#{yield(:title)} | " if content_for?(:title) %>GreenBowl</title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "GreenBowl" %>">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_include_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
    <p class="notice"><%= notice %></p>
    <p class="alert"><%= alert %></p>

    <div id="user_nav">
      <% if customer_signed_in? %>
          Signed in as <%= current_customer.email %>. This cannot be cheese?
          <%= link_to "Sign out", destroy_customer_session_path, :method => :delete %>
      <% else %>
          <%= link_to 'Register', new_customer_registration_path %> or <%= link_to 'Sign in', new_customer_session_path %>
      <% end %>
    </div>

    <%= yield %>
  </body>
</html>
